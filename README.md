# dng2jpg
A simple Python GUI tool for converting a folder of **DNG (Digital Negative)** raw image files into **JPEG** images on Ubuntu.   The application uses a minimal Tkinter interface to let you select a folder, then converts all `.dng` files it finds into `.jpg` files in the **same folder**.
